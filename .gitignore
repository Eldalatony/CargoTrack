# --- Environment -----------------------------------------------------------
.env
.env.*
!.env.example

# --- Dependencies ----------------------------------------------------------
node_modules/
.pnp
.pnp.js
.yarn/

# --- Build output ----------------------------------------------------------
dist/
dist-worker/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# --- Test / coverage -------------------------------------------------------
coverage/
.nyc_output/
*.lcov

# --- Prisma ----------------------------------------------------------------
# Migrations ARE committed (schema is version-controlled — roadmap Gate 1).
# Only generated client artifacts are ignored.
backend/prisma/generated/
backend/src/generated/

# --- Runtime / uploads -----------------------------------------------------
# Anchored: a bare `storage/` also matched backend/src/common/storage, the
# source of StorageModule, which kept it out of every clean clone.
/storage/
backend/storage/
uploads/
*.sqlite
*.sqlite3

# --- Logs ------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Editors / OS ----------------------------------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- Misc ------------------------------------------------------------------
.cache/
tmp/
temp/
